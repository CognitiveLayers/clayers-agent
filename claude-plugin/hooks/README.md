# Plugin Hooks

Reserved for the Phase 2 plugin-hooks work.

Do not add active hooks until they are opt-in, configurable, and tested against normal Claude Code edit flows. The first release should not surprise users by running expensive checks after every write.
