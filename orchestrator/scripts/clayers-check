#!/usr/bin/env bash
set -euo pipefail

orchestrator_root="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
repo_root="$(CDPATH= cd -- "$orchestrator_root/.." && pwd)"

cd "$orchestrator_root"
"$repo_root/claude-plugin/bin/clayers-quality-suite" --no-fix clayers/orchestrator
